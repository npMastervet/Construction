# Construction frontend — public values only; never add secrets here.
# /api uses the Vite proxy in development. Production must proxy /api to Express,
# or set an absolute HTTPS backend URL before building.
VITE_API_URL=/api
VITE_APP_NAME=Construction Cost Manager
VITE_APP_LOGO_URL=/branding/logo.svg
